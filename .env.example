# Environment configuration example for Scry_Ingestor.
# Copy this file to `.env` (or `.env.local`) and adjust the values before
# running services locally or via docker-compose.

# Core application settings
SCRY_ENVIRONMENT=development
SCRY_LOG_LEVEL=INFO

# Database & caching
SCRY_DATABASE_URL=postgresql+psycopg://scry:scry_dev_password@postgres:5432/scry_ingestor
SCRY_REDIS_URL=redis://redis:6379/0

# Security
SCRY_API_KEYS="dev-key"

# Messaging / observability
SCRY_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
SCRY_KAFKA_TOPIC=scry.ingestion.complete
SCRY_AWS__REGION=us-east-1

# docker-compose managed Postgres credentials (passed through to the container)
POSTGRES_DB=scry_ingestor
POSTGRES_USER=scry
POSTGRES_PASSWORD=scry_dev_password
